---
title: "test doc for 9am"
author: "kaitlyn"
format: html
editor: visual
---

## Set up

```{r}
#| message: false

library(tidyverse) # general use package
library(lterdatasampler) # package of datasets
library(NatParksPalettes)

data(pie_crab) # crab size dataset
```

# Exploratory data visualization

```{r}
ggplot(data = pie_crab,
       aes(x = latitude,
           y = size,
           color = site)) +
  geom_point() +
  # changing colors to be different from default
  scale_color_manual(
    values = natparks.pals("Arches", n = 13, type = "continuous")
  ) +
  # adding theme
  theme_minimal()
```

